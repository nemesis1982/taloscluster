apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: cv-server
  namespace: flux-system
spec:
  interval: 10m
  path: clusters/main/kubernetes/management/cv-server/app
  prune: true
  sourceRef:
    kind: GitRepository
    name: cluster

