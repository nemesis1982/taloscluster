apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: daisyfrontend
  namespace: flux-system
spec:
  interval: 10m
  path: clusters/main/kubernetes/demo/daisyfrontend/app
  prune: true
  sourceRef:
    kind: GitRepository
    name: cluster

