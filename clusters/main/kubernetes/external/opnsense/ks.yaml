apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: opnsense
  namespace: flux-system
spec:
  interval: 10m
  path: clusters/main/kubernetes/external/opnsense/app
  prune: true
  sourceRef:
    kind: GitRepository
    name: cluster

