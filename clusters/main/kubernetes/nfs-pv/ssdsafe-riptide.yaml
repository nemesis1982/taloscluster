apiVersion: v1
kind: PersistentVolume
metadata:
  name: ssdsafe-riptide-nfs-pv
spec:
  storageClassName: ssdsafe-riptide-nfs
  capacity:
    storage: 1Mi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  nfs:
    server: ${NFS_IP}
    path: ${NFS_PATH_SSDSAFE_RIPTIDE}
  mountOptions:
    - nfsvers=4
    - tcp
    - intr
    - hard
    - noatime
    - nodiratime
    - nolock