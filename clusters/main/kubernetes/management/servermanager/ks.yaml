apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: servermanager
  namespace: flux-system
spec:
  interval: 10m
  path: clusters/main/kubernetes/management/servermanager/app
  prune: true
  sourceRef:
    kind: GitRepository
    name: cluster

