apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: unifi
  namespace: unifi
spec:
  interval: 10m
  path: clusters/main/kubernetes/management/unifi/app
  prune: true
  sourceRef:
    kind: GitRepository
    name: cluster

